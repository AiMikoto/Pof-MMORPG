#pragma once
#include <vector>
#include <string>
#include <fstream>
#include <iostream>

namespace graphics {
	std::vector<std::string> charArrayToStringVector(const char* const input[], size_t arrayLength);
}